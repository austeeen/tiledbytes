name: unit-tests

on: [push]

jobs:
    unit-tests:
        runs-on: ubuntu-latest

        steps:
            - name: "grab commit"
              uses: actions/checkout@v2

            - name: "run-unit-tests"
              run: ./run bu
